using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class loadingscreen : MonoBehaviour
{
    static string[] jokes;

    void Start()
    {
        jokes = new string[]{"trying to fix my headphone for 4 minuets and 33 seconds", "applying glue on a tuba"};
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
